name: 'Cache all Android dependencies'
description: 'Install and cache all Android dependencies'
runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/cache/npm
    - uses: ./.github/actions/cache/gems
      with:
        working-directory: android
